import { useMutation } from "react-query";
import { authApi } from "../../../Api/Auth/auth";

// const useLogin = (email: string, password: string) => {
//   return useMutation(authApi.login(email,password),);
// };

// export default useLogin;
